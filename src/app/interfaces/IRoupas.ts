export default interface IRoupas {
    outfit? : string,
    id? : number,
    nome : string,
    tipo : string,
    caminhoImagem : string,
    display? : string,
    usuarioId? : number,
    deleteable? : boolean,
    acessorio? : boolean
}