export default interface IRoupas {
    id? : number,
    nome : string,
    tipo : string,
    caminhoImagem : string,
    display? : string,
    usuarioId? : number,
    deleteable? : boolean,
    acessorio? : boolean
}